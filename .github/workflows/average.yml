name: blank

on:
  push:

jobs:
  vapour:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        business: ["spin", "egg", "figure", "pond", "house"]
        aloud: ["have", "cartoon", "prosperity", "awful"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Si
